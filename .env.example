# ============================================================
# AI 模型配置
# ============================================================

# AI Provider (支持多模型竞赛用几个就填几个，用逗号分隔: "deepseek,claude,gemini,openai")
AI_PROVIDER=openai,qwen,deepseek

# 统一API密钥 - 到这个网站申请: https://ai.jlbcode.info/
# 注册账号后创建令牌，一定要在平台开通对应模型的使用权限
FASTAPI_KEY=

# OpenAI
OPENAI_MODEL=gpt-5.2

# DeepSeek
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-3.2
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Google Gemini
GEMINI_MODEL=gemini-3-flash-preview

# Qwen
# Base URL 按照 API Key 所属地域选择：
# - 华北2（北京）：https://dashscope.aliyuncs.com/compatible-mode/v1
# - 新加坡：https://dashscope-intl.aliyuncs.com/compatible-mode/v1
# - 美国（弗吉尼亚）：https://dashscope-us.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-max

# Anthropic Claude
CLAUDE_MODEL=claude-opus-4-5-20251101

# ============================================================
# Polymarket 配置
# ============================================================

# 真实交易模式必需：钱包私钥（以0x开头的64位十六进制字符串）
# 多账户支持：用逗号分隔多个私钥，与 AI_PROVIDER 数量对应
# 例如: POLYMARKET_PRIVATE_KEYS="0xabc...,0xdef...,0x123..." 对应 AI_PROVIDER="deepseek,gemini,openai"
# 单账户模式：使用 POLYMARKET_PRIVATE_KEY（向后兼容）
POLYMARKET_PRIVATE_KEYS=
POLYMARKET_PRIVATE_KEY=

# 钱包类型：auto / eoa / magic_link / proxy / safe
# - eoa: 使用 EOA 签名（signature_type=0），无需显式 funder
# - magic_link/proxy: 使用代理钱包签名（signature_type=1），建议配置或自动派生 funder
# - safe: 使用 Gnosis Safe 签名（signature_type=2），需提供 SAFE 地址作为 funder
POLYMARKET_WALLET_TYPE=auto

# 高级选项：手动覆盖签名类型（0=eoa,1=proxy,2=safe），一般不需要改
POLYMARKET_SIGNATURE_TYPE=2

# 可选：资金地址（代理钱包/SAFE 地址）；EOA 钱包可留空
POLYMARKET_FUNDER=

# ============================================================
# 交易策略配置
# ============================================================

# 模拟交易模式（推荐用于测试多模型竞赛）# true=模拟交易，false=真实交易
SIMULATION_MODE=true
# 模拟模式下每个模型的初始虚拟账户余额
INITIAL_BALANCE_USD=500
#每笔最大交易额
MAX_NOTIONAL_USD=50
#最大仓位
MAX_POSITION_USD=200
#每天最多交易次数
MAX_DAILY_TRADES=200
#间隔多久调用AI
RUN_INTERVAL_SEC=100

# 自动赎回（仅真实交易模式生效）
# 开启后会定期扫描可赎回仓位并发起 redeem
AUTO_REDEEM_ENABLED=true
# 扫描周期（秒，最小 5）
AUTO_REDEEM_INTERVAL_SEC=60
# 最小赎回金额（USD，低于该值跳过）
AUTO_REDEEM_MIN_VALUE_USD=0.01
# 是否优先通过 Polymarket relayer 赎回（EOA 失败时可回退链上直赎）
AUTO_REDEEM_USE_RELAYER=true
# 可选：自定义 relayer 地址
POLYMARKET_RELAYER_URL=https://relayer-v2.polymarket.com/
